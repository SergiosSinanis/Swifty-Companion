import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // private constructor (nobody should ever instantiate this = it's just consts)

  static const Color background = Color(0xFF12141C);
  static const Color surface = Color(0xFF1B1E29);
  static const Color coral = Color(0xFFFF6B57);
  static const Color teal = Color(0xFF3FBFAE);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B4C0);
}
